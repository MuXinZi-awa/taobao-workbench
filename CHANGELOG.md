# 更新日志

> 优化管理工作台 · 更新记录。格式：版本 + 日期 + 分类条目（新增/优化/修复）。

---

## v0.7.0 · 2026-09-10（两款主题 + 万相台报表插件）

### 新增
- **新主题 `苍银`（夜用）**：夜蓝底 × 金印，深色高对比——适合夜间/暗环境；金只用于强调与激活（#C9A227），圆角 4px 方正
- **新主题 `晓蓝`（日用）**：素雅浅蓝 + 磨砂玻璃（`backdrop-filter`）——晨光渐变底 + 半透面板，适合办公室明亮环境；强调蓝调浅（#5E8AB4）
- **主题区改版**：3 列卡片 + 每套一句介绍（照 Hana 风格），布局不再拥挤；主题变量经 `wb-theme` 广播，插件 iframe 自动跟随
- **新插件 `adreport`（报表 📊）**：万相台报表台，把要翻很深才看得到的数据拉平到一处
  - 账户概览 10 指标（花费/成交额/投产比/展现/点击/CTR/平均点击花费/转化率/成交笔数/加购）
  - **指标可点切换走势**（点哪张卡，折线图换主角）；**本期 vs 上期**对比（近7天/近30天/自定义≤30天）
  - **推广计划表（11 个计划）** + **营销场景表**，**均跟随时间范围**切换
  - **今日**范围（走实时接口 `fromRealTime`，日粒度不含当天）
  - 趋势解读（高点 / 挂零日 / 投产比 / 场景集中度）
  - **采集方式**：面板「刷新」= 后台**静默采集**（headless，不弹浏览器）；**惰性定时**（每天 9:00/21:00 后首次打开自动采）
- 内核 API：插件 server `report` / `refresh` 两个 action

### 修复
- 报表插件：表头右对齐、数据左对齐导致的**列错位**（`td` 补 `text-align:right`）
- 报表插件：今日各计划误用账户总计（漏 `strategyCampaignIdIn`）；今日场景误用全区间——已修，并加采集自检（各计划数值全同则告警）

---

## v0.6.0 · 2026-09-10（连接导航 · 凭据管理）

### 新增
- **新插件 `odoo-erp`（ERP 数据）**：Odoo **只读**查询——料号 → 售价/成本/库存/品牌/描述/分类（仅 authenticate/search_read/search_count，绝不写生产库）
- **连接导航**（设置 → 连接）：多连接管理（淘宝店铺 / Odoo ERP），可新增/编辑/激活/测试/删除
- **凭据加密**：密码走 **Windows DPAPI**（ctypes CryptProtectData——可逆、绑当前用户、零依赖）——**绝不明文/不用 MD5**（MD5 不可逆，登录需明文，不可用于此）
- **存储**：`data/conn.db`（sqlite3 标准库，零下载）
- **Odoo ERP 测试连通**：`xmlrpc.client` 标准库直连 `/xmlrpc/2/common` 认证——为接入公司 ERP 产品数据铺路（替代爬取）
- 内核 API：`/api/conn/list|save|del|activate|reveal|test`

---

## v0.5.0 · 2026-09-09~10（送修/换源闭环 + 图库接入）

### 新增
- **素材审核（预览 Tab）复审模式**：待复检品自动显示送修/换源产物 + **⇄ 对比原图**按钮（ref=产物 vs orig=原图）；键盘 ←/A/→/D 切品、**空格**=对比、**F**=force、1/2/3=放行/送修/换源、Enter=确认；审核激活自动拿焦点（不用先点预览）
- **送修闭环**：审核标送修 → 🔁 送修处理（repair_batch v2 **并行**并发 3）→ AI clean 模式（去标注保留原样）→ `_白底_待复检` 落素材 → 标待复检 → 复检。workbench 固定进度条 5s 轮询（送修中 N/M + 当前品 + 失败数）
- **换源闭环**（switch_src）：四源递进 **① 本地图库 `F:\连接图图库` → ② TE 官网 → ③ RenhotecRF → ④ 立创**；命中落 `_库源N_待复检`/`_待复检`；Renhotec 顺手抓 Drawing PDF 当规格书
- **本地图库索引**（lib_images，13.2 万图 / 4.4 万料号，0.9s 建）：料号归一化匹配（连字符无关），查询/审核/换源三处接入
- **单品查询走分流**：单料号回车 = 搜商品 + 查双百 + 素材 + type/audit（不再是纯本地查询）
- **批锁**：送修/换源互斥——一个批在跑时再启动会被拒（防进度文件互覆盖）

### 修复
- **审核 0 素材 bug**：素材目录无图时 `_ref_for` 加图库兜底——审核仍能看到产品图（不再空白）
- **审核 ref 过期快照**：审核启动时对在途品（待复检/换源）现场重查 `audit-ref`——送修完成后不再显示旧图
- **skip 品复检被挡**：双百跳过但有在途标记（送修/待复检/换源）的品不再自动跳过
- **classify 缺 audit**：分流结果带出 audit/ref/orig——送修/换源按钮正确联动
- **lcsc.py 域名拼错**：`item.slcsc.com` → `item.szlcsc.com`（立创通道长期失效真凶）

### 修复
- **tg-monitor 登录态持久化**：内核 call_plugin 每次请求重载 server.py → 模块级缓存丢失（点了检测仍显示"未检测"）→ 登录态落文件 `login_state.json`
- **odoo-erp 精确匹配开关常驻**：原来随结果数消失（结果变 1 条就回不去）→ 钉在查询栏

### 优化
- 素材列显示：只缺图且有库源 → 标 `缺:主图[库N→可补]`（不算硬缺）
- **架构：脚本入插件**（`plugins/pipeline/scripts/`）——repair/switch/lib_images/renhotec/ai_schedule/lcsc/_grab_te_img/_mtop_api 自包含（TG 硬编码清除）；工作台不再依赖外部仓
- 版本号跟随：内核 v0.5.0 / 插件 pipeline·dual-check·tg-monitor 0.2.0
- runtime 脚本入库（gitignore 白名单 `!runtime/*.py`）

---

## v0.4.0 · 2026-09-08（主题全局化 + 推广监控工程化）

### 新增
- **主题全局跟随机制**：宿主切主题 `postMessage` 广播 CSS 变量 → 插件 iframe 接收应用——三插件全部跟着主题走（纸色 fallback 保底）
- 新主题：**深邃蓝**（delve 冷白+纯黑高对比致敬）、**莫兰迪**（雾蓝灰低饱和）——含组件级形态覆盖（圆角/阴影/层次）
- **推广监控台插件**（tg-monitor）：
  - 11 计划组真名卡片 + 容量实时（probe 拉登录态连带刷新 + 「刷新容量」按钮）
  - **推广工程区**（与监控区隔离）：单品精准推广（料号/ID）/ 批量（拖 csv 或默认池）/ 数量可调 / 停止
  - 默认池进度（共 4284 / 已跑 / 剩）+ 本次批料号清单 + 批结束自动提示新增数
  - 起停批 PID 方案（tasklist 检测，跑批状态可靠）
- 查双百单查重排（卡片式上下布局 + 整宽查按钮）

### 修复
- 推广假成功三连根因（脚本侧，详见 taobao 仓）——本面板口径已按权威记录显示
- Chrome autofill 灰蓝底盖输入框 → `-webkit-autofill` 强制跟随主题底色
- 插件反复 patch 遗留的重复函数/写死色 → 变量化清理
- 登录态探测卡死进程残留导致连环失败（profile 锁）——进程清理 + 90s 缓存
- col-desk / 面板区域特殊白 → 统一 `var(--card)`（区域不再掉队）

### 优化
- 插件 iframe 缓存并存（切换不销毁——各插件独立存活不重载）
- 插件 icon 生效（manifest `icon` emoji）
- 拖放区加大（130-140px）三插件统一
- 流水线阶段灯居中布局

---

## v0.3.0 · 2026-09-07（工作台成型 + 独立仓库）

### 新增
- 四区布局工作台（书桌/工作区/运行日志/预览）+ 设置体系（主题/字体/字号/编辑器 md 参数/浏览器登录态/通用自启/插件开关）
- 插件体系：zip 拖入即装（manifest+panel+server+README）、热插拔、server 动态加载
- 查双百 v2：单查（料号/ID）+ 批量拖表（csv/xlsx 自动识别料号列 → 后端单连接逐行查 → 进度 + 总结）
- 预览全家桶：图片缩放拖动 / PDF fitz 转图 / CSV 表格 / MD 渲染
- 编辑器 Markdown 参数：字体/字号/宽度/H1-3/行高/边距/恢复默认
- 独立仓库 `taobao-workbench`（公开）+ 本地 git

### 修复
- 宽度按钮"点不动"（重复 edSeg 定义覆盖 → onclick 拼错）
- manifest `panel` 字段笔误（`entry` → 点开 404）
- 误删 edSet/edNum/EDKEY 补回、重复声明清理

---

## v0.2.0 · 2026-09-06（内核对齐 + 插件雏形）

- 内核搜索/文件 API / 书桌素材联动（料号 → 显示素材）
- 查双百插件雏形（单查）
- 设置-浏览器：登录态检测/引导登录/清除 Cookie

---

## v0.1.0 · 2026-09-05（内核起步）

- workbench.py 标准库内核（8900 端口）+ index.html 初版
- 书桌文件浏览/预览基础
