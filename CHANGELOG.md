# 更新日志

> 优化管理工作台 · 更新记录。格式：版本 + 日期 + 分类条目（新增/优化/修复）。

---

## v0.8.29 · 2026-09-20（pipeline 排版分区 + 新增「改库存」插件 + 日志进 Tab）

### 新增
- **`stock-edit` 插件（改库存）**：全店/指定品改库存与售价
  - 读全店清单（按激活账号隔离）；`--gap` 默认 2.5s；**断点续跑**（按留痕跳过，第二次不再全量遍历）
  - 改售价：成本**优先现查 ERP**（odoo-erp 通道），查不到回落本地表并在结果里标来源
  - **硬护栏：老品不许降价**（低于原价就跳过并写明原因）；认品**只认带后缀的完整料号**（`outerId`）
  - 结果表分色（✓通过 / ⏭跳过 / ✗失败）+ 逐条原因；留痕 `runtime/stock_edit.csv`

### 优化
- **`pipeline` 面板排版分区**：顶部控制行/类目行/阶段区/表格各成一张卡，`data-zone` 打头，统一 8px 竖节奏
- **插件日志进「运行日志」Tab**：`stock-edit` 的 `manifest.log` 指向 `runtime/stock_edit_run.log`，面板底部不再堆日志
- **目录与运行产物清理**（`stock-edit` 只读 `推广一键跑/runtime`，不往工作台写产物）

### 修复
- **旧横幅“赖着不走”**：`pipeline.repairBar` 只看内容→多天前的「送修批结束」天天冒
  - 服务端补 `mtime/age_sec/stale`；面板只认「真在跑」或「本轮才结束的」
- **`stock-edit` 列对齐**：`table-layout:fixed` + `colgroup` 定宽，数字列右对齐
- **模式切换 UI 不同步**：改什么选售价/两者时，值标签/单位/默认值/提示跟着变
- **「本次结果」切不回**：空结果也要渲染，否则会“卡在”历史留痕

---

## v0.8.28 · 2026-09-19（起批切接口版 + 全量输出落盘 + 运行产物出库）

### 修复
- **起批切到接口版 `tuiguang_api.py`**（`scheduler` / `tg-monitor`）：`promo_batch` 与面板起批的 `script` 从 `tuiguang_auto.py` 改指 `tuiguang_api.py`
  - `--campaigns` **留空** → 由接口自取「未满」计划（写死计划列表会带上已满的）
  - 实测：**30 品 2 分 37 秒**（29 成功 / 1 跳过 / 0 失败），锁只占 2.5 分钟
  - 对比：此前 UI 版约 **48 分钟/批 + 锁占 80 分钟**，会把报表任务顶掉
  - 踩坑（今天的）：**配置改了但没生效**——`daemon` 是常驻进程，动作表缓存在内存里，**必须重启守护**才读新配置

### 新增
- **`pipeline` 脚本全量输出落盘** `runtime/stage_out.log`（脚本的关键判断只打在 stdout，不存就没法事后查）

### 备注（如实记账）
- `plugins/pipeline/scripts/lib_index.json`（约 10MB 图片库缓存，由 `lib_images` 扫 `F:\连接图图库` 生成、可重建）及 `odoo_erp.log` / `repair.log` / `repair_err.log` / `repair_state.json` 已 `rm --cached` 移出版本控制，本地文件保留

---

## v0.8.27 · 2026-09-18（进度计时冻结 + 取图只认审核产物 + 强制重做）

### 修复（梓帆实测报的）
1. **批量进度「已跑 N 分钟」不会停**（他盯了 50+ 分钟）
   - 根因：`tg-monitor/panel.html` 无条件用 `Date.now()` 算已跑时间 → 批跑完仍一直涨
   - 修：在跑用 now，不跑/疑似中断 → **冻在 `updated`**
   - 实测：`推广 3/3 · … · 已跑 0 分钟（起 14:20:47 · 更 14:20:56）已结束` ✓
   - **梓帆 0918 复报「流水线里又出现一次」**——根因：**同一段显示代码存在两份**（`tg-monitor` 与 `pipeline` 各一份），上一次只修了前者。两份同步修
   - 另加：**结束超过 30 分钟就不再展示**（旧记录不赖着）——他看到的“已跑 98 分钟”根本不是那一批，是上一批推广的残留
   - 实测：`pipeline` 面板重新加载后 `#batchprog` display=none、无残留文字 ✓
2. **素材/整理 取的是带水印的脏图，不是已审核的产物**
   - 根因两层：
     a. `make_local.pick_best()` 把**所有生成产物都跳过**（`_封面/_主图/_白底/_详情`）
        —— 连人工换源/送检回来的 `_待复检` 也一起跳了 → 素材目录“找不到图”
        → 掉进**图库兜底**（门槛 40，连“带水印但可用”都收）
     b. 即使产物优先了，仍会用图库图**凑数**（这类品其余素材多半也脏 → 混进来把成品带脏）
   - 修：`_待复检` / `_换源` / `_库源` 提为最高优先（999）；**有产物 → 只用产物**，不再用图库兜底填数
   - 实测：`pick_best('1379029')` → 2 条，全是 `_白底_待复检_lite测试1/2.png`（修前还混着 3 张图库图）✓

### 新增
- 面板「素材」格加 **「强制重做」** 勾选（→ `make_local --force`）
  —— 梓帆原来靠“手动去素材文件夹删图再点”绕开“已生成就跳过”，现在一个勾就行

### 铁律（梓帆 0918 定，已记入长期记忆）
- 换源/送检修图**只送一张**，**绝不混入其他素材**——这类品其余素材多半也是带水印的脏图
- 素材生成同理：有审核产物时**只用产物**，不用图库兜底凑数

---

## v0.8.26 · 2026-09-18（批量进度显示 + 容量预检修复 + 脚本清单）

### 新增
- **批量进度显示**：跑批时写 `推广一键跑/runtime/progress.json`
  （`kind/total/done/ok/skip/fail/current/started/updated/running`，每品在循环头更新一次）
  - 埋点：`tuiguang_api.py` / `tuiguang_auto.py` / `_xinpin_shangpin.py` / `_batch_optimize.py`（含 EXE 树）
  - 显示：`tg-monitor` 面板顶部一行「推广 7/30 · 当前 xxx · 成功6 跳过1」（在跑 3s 轮询 / 空闲 10s；
    `running && now-updated<120s` 才算在跑，否则显示「疑似中断」「已结束」）
  - `pipeline` 面板也读同一份
  - 验证：真跑 `--limit 6`，`done` 0→6 单调递增、`current` 逐件切换；面板运行态/疑似中断分支均实测

### 修复
- **`tuiguang_api.py` 容量预检失效**：读错字段（`data.totalResults` → 实为 **`data.count`**，参见 `tuiguang_auto.campaign_count`）
  —— 后果：预检永远拿 -1，**计划满了不会自动切**，且把 `plan_state` 写成 0（面板容量偏低的根因）
  - 实测：修后 `findPage → data.count = 345`（计划 83259606767）

### 文档
- 新增 `工具/脚本清单_推广一键跑.md`：**顶层 82 个 .py**（不是先前说的 200+；`runtime/` 是 venv、`_archive/` 是 115 个归档）
  - 主线 6 条 + 全量清单（作用/谁调/状态）＋ 状态分布 ★常用39 / 备用26 / 一次性14 / 疑似过期3
  - 三条要点：`_mtop_api.py` 是双百与登录的地基；推广池靠 `_make_tg_pool.py` 清洗；**三个总装并存且面板连着最老的那个**

### 备注（如实记账）
- 进度显示验证期间多跑了 6 次接口批，**新建 26 个真实推广单元**（均为池内待推广品、均已写推广记录）

---

## v0.8.25 · 2026-09-18（推广接口正式版 + EXE 副本同步）

### 新增
- **`推广一键跑/tuiguang_api.py`**：接口版推广批（不走 UI 点选）
  - 参数与 `tuiguang_auto.py` 对齐（`--excel / --limit / --campaigns / --bid / --no-pop`）—— scheduler 可直接换
  - 每品：`material/accessAllowed.json`（可投校验）→ `adgroup/addList.json`（建单元）
  - **每品只选「料号关键词」一条**；`wordPackageList` / `crowdList` **置空**（不选词包、不选人群）
  - 容量预检 `adgroup/horizontal/findPage.json`；满 500 自动切下一个计划，并回写 `plan_state_<mid>.json`
  - 判重：返回体 `errorDetails` 含「已绑定/无需重复」→ 记 `已投放跳过`
  - 模板：`runtime/tuiguang_api_template.json`（首次运行从抓包生成，之后可手改）
- **实测**：`--limit 1` → 10 秒完事（含起浏览器），**建单元本身 5 秒**；返回 `adgroupId`、`addWordPackageResult: null`、`addCrowdResult: null` ✓

### 同步 / 清理
- **EXE 副本同步主树**：5 个文件（`_report_fetch` +61 行 / `_report_today` / `organize` +2 / `report_db` +151 / `tuiguang_auto` +82）——EXE 原文件备份在 `EXE\推广一键跑\_bak_sync_0918\`
- 死料号 `0-1123721-1`（商品不存在，连续两天卡在「商品搜索结果」36s）→ 补记录标 `无此商品`，池子不再取它

### 待确认
- **单元的关键词未能在平台侧直接读回**：`adgroup/horizontal/findPage` 无关键词数字段；关键词 tab 能看到该单元，但展不开词列表。
  结构上我们发的 `wordList[0]` 与真实 UI 报文**同形**（`{word:料号, bidPrice:"0.3", onlineStatus:1}`），但**需要人工瞄一眼单元的关键词**才算坐实。
- 确认前**不接进定时批**（`tuiguang_api.py` 目前无任何调用方）

---

## v0.8.24 · 2026-09-18（推广接口 POC 跑通 · 建一个单元 4 秒）

### 结果
- **用接口建推广单元成功**：`POST one.alimama.com/adgroup/addList.json` → HTTP 200、返回 `adgroupId=83616008231`、`errorDetails: []`
- 耗时 **10 秒**（含启动浏览器），UI 老路一品 60~90 秒
- 产出：`推广一键跑/tuiguang_api_poc.py`（模板 = 抓包真 body，只替换 csrfId/loginPointId、计划、商品、词+出价四处）
- 接口文档 `工具/万相台接口-创建推广单元.md` 补「POC 结果」一节

### 实测结论（已写进接口文档）
- `material/accessAllowed.json` 可预判该品能否推广（`accessAllowed: true/false`）
- 重复提交已推广的品 → HTTP 200 + `errorDetails[0].msg` 含「已绑定，无需重复推广」→ **可判重**
- 选中的词 = `wordList[0].word`，值是**纯料号**；`make_keywords` 那套方案词只用于 UI 搜索

### 补账
- 抓包/POC 期间真建出 3 个单元（`1-1418507-2` / `1-967623-6` / `1981731`）——抓包脚本直调 `do_tuiguang` 不写记录，已手工补进 `推广记录.csv`（否则今晚那批会重复去推）

---

## v0.8.23 · 2026-09-18（万相台「创建推广」接口抓包 · 可绕开 UI）

### 抓包结果（真跑一次完整流程，288 条报文）
- **创建接口 = `POST https://one.alimama.com/adgroup/addList.json`**——一次请求建完单元（商品 + 词包 + 出价 + 人群）
- 通用约定：`one.alimama.com/<模块>/<动作>.json` 全部 POST JSON，body 必带
  `bizCode`（`onebpSearch` / `universalBP`）+ `csrfId`（`<hex>_1_1_1`，每次会话变）+ `loginPointId`；鉴权靠浏览器 cookie
- 完整链路：`material/item/findPage` → `material/accessAllowed` → `bidword/suggest/kr/list` → `crowd/*`（可选）→ **`adgroup/addList`**
- 容量预检可用 `adgroup/horizontal/findPage.json`（pageSize=1 读 totalResults）
- 产出文档：`OH-WorkSpace/工具/万相台接口-创建推广单元.md`（各端点完整 body）
- 原始抓包（含未被截断的 addList 完整 body，42601 字符）：`推广一键跑/runtime/_api_capture.json`

### 验证
- 抓包时真建了推广单元（`1-1418507-2` 走完全流程「推广创建完成」）——都是待推广品，属正常推广动作
- 目标品 `0-1123721-1` 连续两天卡在「商品搜索结果」，**该品单独标记待查**

### 下一步（POC）
- 用已验证的「关键词方案 / 出价 0.3 / 容量预检」组参，先在**页面上下文 `fetch`** 打 `adgroup/addList.json`
  （同源 cookie + 现抓 csrf，最省事）；跑通后再谈完全脱离浏览器

### 备注
- 注释规范化清扫：已委派（阿尔托莉雅），只改注释文字、不改代码；报告待回

---

## v0.8.22 · 2026-09-17（有 AI 返图的品·参考图为空）

### 修复（梓帆：这个产品有 AI 送修返图，为什么看不到参考图）
- **现象**：`1379029` 文件夹里明明有 `1379029_白底_待复检_lite测试1/2.png`，面板/审核却 **ref 为空**
- **根因**：`_ref_for()` 的逻辑是「audit ≠ 放行 → 看待复检产物；否则 → 看原图」
  —— 而这个品送修完成后 `audit` 被写成「**放行**」+ note「送修完成」→ 走原图分支；
  **但它根本没有原图**，只有 AI 返图，而返图文件名含「白底/待复检」又被排除 → 落到图库兜底 → 空
- **修**：原图分支找不到时，**再认一轮产物**（`*_待复检*` → `*白底*` → `*_库源*` → `*_换源*`）→ 才落图库
- **验证**：`/scan?lhs=1379029` → 修前 `ref=''`，修后 `ref='...1379029_白底_待复检_lite测试1.png'` ✓

### 完成（梓帆 0917 定：素材走**本地**秒级，AI 只用于送修 → 已接）
- **素材格**：从「只清点」→ **本地一条龙补齐** `make_local.py <料号>`
  （封面 + 主图2-5 + 详情图HTML + 视频ffmpeg；已生成自动跳过，`--force` 才重跑）
- **整理格**：`organize.py` 落位 **+ 齐全性校验**（封面/详情图/视频/规格书）
  —— 顺手把 `scan_material` 的缺失清单补上「详情图」（原来只报 封面/主图/规格书/视频）
- 验证（挑已全套料号 `1-178128-2`，**零副作用**：已生成→跳过）：
  - `/run?stage=material` → `本地一条龙：1-178128-2✓｜仍缺：…→规格书` ✓
  - `/run?stage=sort` → `完成 -> runtime\整理结果\素材整理结果_0917_1804.csv｜齐全校验：缺：…规格书` ✓

### 待办（梓帆 0917：“我确实是想用接口”——推一个品走 API，不走浏览器）
- 现有能力：`_mtop_api.MtopClient.call(api, data, v)` 是**通用签名调用器**（`_sign()` 已通）
- 万相台侧：`one.alimama.com` 的 json 接口（`report/query.json` / `campaign/findList.json`，靠页面抓 `csrfId`）
- **实施路径**：先抓一次浏览器建推广的请求（URL + csrfId + body）→ 用已跑通的「关键词方案/出价/容量预检」逻辑组参 → 接口直接建
- 待梓帆定：先做「建一个推广单元」的 POC，还是先抓全链路（建方案+单元+词+出价）

### 插件
- `pipeline` 0.2.13 → 0.2.15

---

## v0.8.21 · 2026-09-17（登录预热加进程级缓存 · 梓帆当场抓到效率塔方）

### 修复
- **症状**：登录态可用、计划全满时，脚本**每切一个计划就重验一次登录态** → 遍历计划组从「秒级」变成「一个计划 ~8 秒」（梓帆原话：效率指数级打折扣）
- **根因**：`warm_login` 被放进 `_plan_ready`，而 `_plan_ready` 在「满 500 切下一个计划」的循环里**每次都会走** → 一次性成本变成每轮成本
  （实测日志：`登录态本就好用（卖家中心直达）→ 转万相台` 与 `已满(500)→预检切下一个` 交替刷）
- **修**：`warm_login` 拆成**带缓存的入口** + `_warm_login_once()`；进程级 `_WARM={done,ok}`
  —— 一个进程 = 一次 launch = 一份登录态，验一次就够
- **验证**：第 1 次 `warm_login` = True / **2.53s**；再调 10 次（模拟切 10 个计划）= **0.000s** ✓

### 经验
- **一次性成本绝不能放进循环**：预热/初始化/探测这类“每进程只需一次”的动作，进循环前先数它会被调几次
- 另注：17:48 那批（pid=22028）中途死亡、锁文件残留但 **OS 锁已释放**（msvcrt 进程死即释放）——`.holder` 留着旧内容不影响下次抢锁

### 文件
- `推广一键跑/tuiguang_auto.py`（登录预热缓存）
- `OH-WorkSpace/_设计_登录合一_0917.md`（新增；合一方案 + 本条的缓存要求）

---

## v0.8.20 · 2026-09-17（流水线三修 · 素材列消失 / 双百混入审核 / force 残留）

### 修复（梓帆 0917 下午实测反馈）
1. **「素材」列点一次查询就消失**
   - 根因：`_classify_one.py` 只回 `{lh,id,title,type,dual,note}`——**没有 `素材` 字段**；前端 `render` 是 `it.素材==='齐'?…:cell(it.素材)` → `undefined` 渲染成空白
   - 与 0915 那次「类型被冲成查询异常」是**同一类病**：某个操作回吐的「半截行」覆盖了完整行
   - 修（`pipeline/server.py` classify 分支）：以 `scan_batch`（本地素材清点 + state 标记）为底，用分流结果覆盖 type/id/title 再回前端
   - 验证：`/classify?lhs=1379668` → 返回字段含 `素材='齐'`、`审核='放行'`、`上品/优化`、`推广`、`缺列表` ✓
2. **双百跳过的品混进审核队列**（截图实锤：审核 2/2（需审 1）DT06-12SA 标着「(双百-已跳过,force纳入)」）
   - 根因两层：
     a. `panel.html` 的 `adStartFrom` 把 skip 的品也 push 进队列，只靠前端「翻页时跳过」
     b. **`index.html` 的 `AD.force` 从不重置**——`adStart()` 只重置 items/idx/marks；上一批点过一次 force（或手滑按了 `f`）→ 之后每批默认 force → 跳页保护全失效
   - 修：`panel.html` **skip 的品直接不入队**（在途品 送修/待复检/换源 不算 skip，仍要复检——原正则保住）；`index.html` 的 `adStart` 重置 `AD.force=false` + 勾选框复位
   - 验证：两文件 `typeof adStart/adStartFrom` 均 function（脚本解析正常）、内核 `AD.force` 默认 false ✓
3. **`organize.py` 每次跑往桌面丢一张 CSV**（梓帆：点一次多一张，受不了）
   - 修：输出改成 `推广一键跑\runtime\整理结果\`（保留时间戳留痕）

### 新增（梓帆方案 · 万相台登录「绕弯」· **已验证成立**）
- `tuiguang_auto.py` 新增 `SELL_URL`（卖家中心商品页）+ `warm_login(page)`：
  ① 进卖家中心商品页 → ② 若被弹到登录页，**就在千牛这边把登录做掉**
  （`wxt_login_if_needed` 能识别 `havanalogin.taobao.com/mini_login.htm` 的 `#fm-login-id`，密码登录通，没被验证码卡）
  → ③ 等它自己跳回卖家中心 → ④ 再转万相台
- 已插入两处导航（`do_tuiguang` / `_plan_ready`）；原 `wxt_login_if_needed` 保留为兜底
- **踩坑（差点把方案写歪）**：登录点完后**不能立刻 `goto(SELL_URL)` 去“确认”**——会把登录后的跳转**打断**，白登一次
  （现象：填了密码也点了登录，却又回到登录页）。改成**轮询等它自己跳走**（≤ 30s）
- **验证（调真代码，非复制逻辑）**：`await warm_login(page)` → True；转万相台 → url=`one.alimama.com/.../search-detail?...`、
  **无登录框**、页面已渲染计划列表（关键词1~11）、「添加宝贝推广」= 1 ✓ **绕弯成立**

### 更正
- 上一版曾判定「chrome_profile 登录态已失效、需手动重登」——**撤回**：登录本身是通的，卡点是上面那个「自己打断自己」
- **而且这不是新方案（梓帆 0917 指出，报得对）**：`publish_auto.login_qianniu()` 一直就是这个思路——
  “有「发布商品」按钮=已进搜索页→跳过登录；被弹登录页则填账号密码+回跳”，`SELL_URL` 常量也在那边；
  连「登录点完别 goto、跳转交给浏览器」这条坑，`tuiguang_auto` 第 341 行注释里早就写着
  → 已把交叉引用写进 `warm_login` docstring（以后改登录先看这两处）+ 记入经验库

### 插件
- `pipeline` 0.2.12 → 0.2.13

---

## v0.8.19 · 2026-09-17（pipeline「加血肉」· 七格全通）

### 新增（`pipeline` 插件 · **只加调用层，未动任何底层脚本**）
- 四格从「桩」接成真调用（`ready` False → True）：**属性 / 整理 / 上品·优化 / 推广**
  - **属性** → `推广一键跑/fetch_attrs.py`（临时表 `料号` 列 + N）——**只爬不改商品**；已爬自动跳（属性清单.csv 累积）
    - 注：STAGES 原 desc 写的「→ fill_attrs 回填」**没接**——回填=写商品，放 上品/优化 阶段更合闸门逻辑。**待梓帆确认**
  - **整理** → `organize.py`（临时表 `料号` 列）
  - **上品/优化** → 按 state 的 `type` **自动分流**：`新品*` → `_xinpin_shangpin.py --only <料号>`；`*需优化*` → `_batch_optimize.py <临时表>`
    - `_batch_optimize.load_targets` 读「第 1 列=料号、第 4 列=淘宝ID」→ 临时表按此 4 列表头生成
    - 新品路 `--only` 只在该脚本自己的定价表 ∩ 有素材 ∩ 未 `.新品.done` 的范围内生效（自带防重复）
  - **推广** → `tuiguang_auto.py --excel <临时表> --limit N --campaigns <未满计划> --no-pop`
    - 计划 ID **不写死**：读 `plan_state_<member>.json` 里 count<500 的（与 `scheduler/server.py:auto_campaigns` 同源）；取不到 → **明确拒绝**并提示先刷容量
- 新增后端工具：`_write_tmp()`（临时表落 `runtime/_pl_tmp/`）、`_run_script()`（子进程范式，同 `_classify_one`）、`_types_of()` / `_ids_of()`、`auto_campaigns()`
- 每个阶段跑完**回吐刷新后的状态表**（`items`）——前端 `render(d.items)` 直接刷，**面板一行未改**

### 验证（安全路径，未碰真商品）
| 阶段 | 用例 | 结果 |
|---|---|---|
| attrs | 已爬过的 `1-967628-1` | `ok=True`、msg「全部已爬——无需处理」、回吐 items ✓ |
| tuiguang | 不存在的料号 | `ok=False`、err「state 里没有淘宝ID——先跑查询」✓ 零动作 |
| pub | 不存在的料号 | `ok=True`、msg「跳过 1 个（type 未定/双百跳过）」✓ 零动作 |
| sort | 不存在的料号 | `ok=True`、脚本正常收尾 ✓ 零动作 |

- `/stages` → 7 格全 `ready=True` ✓；`py_compile` 通过 ✓

### 待确认 / 遗留
1. **属性阶段要不要连“回填”**（现为只爬；回填已在 上品/优化 路径里）
2. `organize.py` 每次跑都把结果 CSV **写死到桌面**（`素材整理结果_MMDD_HHMM.csv`）——接进流水线后每点一次「整理」桌面就多一个文件。要不要收进 runtime？（改它=动底层，未动）
3. 面板「上品/优化」列仍为空：`state.items.<料号>.sp` 没人在写——要不要跑完阶段后由插件自己回写（批次级标记，非逐品确认）？
4. 上品/推广是**真动作**，本次未做端到端真跑（需梓帆在场，建议先 1 品 `--no-submit` 预演）

### 插件
- `pipeline` 0.2.11 → 0.2.12

---

## v0.8.18 · 2026-09-17（报表「趋势解读」跟随所选范围）

### 修复（切范围时解读文案不动 —— 2 天范围底下写「区间内 82 天挂零」）
- **修（`adreport/server.py`）**：新增 `insightByRange`（7/30/all 各一份）+ `insightCustom`（自定义窗口），与区间表**同源同窗口**（`_days_win` 按窗口切 `days`、`_tot_of` 现算该窗口汇总）
- **修（`panel.html`）**：解读块按 `MODE` 取对应那份，标题右侧标出当前范围（如「趋势解读  近7天」）
- **今日模式不显示解读**：今日无按日明细（T+1），解读算不出来 → 干脆不显示（原为显示全区间文案，与「今日」自相矛盾）
  - 若想让「今日」也有一句，得改用 `today` 快照口径另写一套 —— 待梓帆定
- 旧字段 `insight`（全区间）保留，兼容旧前端

### 验证（headless 打开 `/panel/adreport`）
| 模式 | 解读标题 | 首条 |
|---|---|---|
| 近7天 | 趋势解读 近7天 | 高点 09-12 · 20.28 |
| 近30天 | 趋势解读 近30天 | 高点 09-09 · 33.92 |
| 自定义 09-15~09-16 | 趋势解读 自定义 09-15 ~ 09-16 | 高点 09-16 · 39.25 |
| 今日 | （不显示） | — |

- 同页对拍：近7天 KPI 194.38 = 计划表 194.38 = 场景表 194.38；自定义 66.13 = 66.13 = 66.13（7 项相加）✓

### 插件
- `adreport` 0.2.8 → 0.2.9

---

## v0.8.17 · 2026-09-16（报表区间表改读「按日明细」· 与 KPI/走势同源）

### 修复（切「自定义范围」下方两张表不跟随；近7天/近30天与 KPI 差一天）
- **症状**：自定义 09/15~09/16 时上方 KPI = **26.88**（2 天），下方「推广计划/营销场景」却还是**全区间 431.08** → 对不上
- **根因（三层）**：
  1. **口径不同源**：区间表读 `plan_snap`/`scene_snap` 的区间快照(7/30/all) **+ 今日快照合并**；KPI/走势读 `daily`（T+1）。实测 全区间快照 410.98 + 今日 20.1 = **431.08**（正是面板上那个数）
  2. **快照没有「自定义」这个键** → 前端只能回落 `'all'`
  3. 附带：快照 '7' 是 `today-6 ~ today`（09-10~09-16），前端 `curDays()` 的 7 天是 `昨天-6 ~ 昨天`（09-09~09-15）→ **差一天**
- **修（`adreport/server.py`）**：
  - 新增 `_win(tag)` / `_range_plans(s,e)` / `_range_scenes(s,e)`：区间表改从 `report_db.get_plan_daily` / `get_scene_daily` **按区间聚合**，与 KPI/走势**同源**；窗口一律 **截至昨天**（T+1，与前端 `curDays()` 对齐），今天不进区间表
  - 前端传 `?cs=&ce=` → 返回 `plansCustom` / `scenesCustom` / `customOk`；**任意 ≤30 天窗口**都能算
  - `report(q=None)` 收 query；`handle` 传 `qs`（兼容 dict / 原始 querystring）
  - 无按日明细的老库 → 回退旧快照合并逻辑（不至于空白）
  - 删除 `if stale:` 里那段用快照覆盖区间的 `for _tag` 循环
- **修（`panel.html`）**：`_rq()` 拼 cs/ce；`applyCustom()` 改调 `load()`（重新取数）；`MODE==='custom' && DATA.customOk` → 取 `plansCustom`/`scenesCustom`，表头显示「自定义 MM-DD ~ MM-DD」
- **`all` 起点钉死 `1970-01-01`**：`report_db` 的区间过滤是 `if start and end:` —— 传空串 = **完全不过滤**（今天/未来行会漏进来），故不能用空起点

### 踩坑（重要）
- 工作台进程会**缓存同名模块**（`sys.modules`）→ 插件里 `import report_db` 拿到的是**旧版**（当时缺 `get_plan_daily`/`get_scene_daily`）→ `AttributeError` 被 `except` 吞掉 → 区间表**静默**退回快照（数据看着「有」，其实口径错）
- 修：插件新增 `_rdb_fresh()` —— **按文件路径 `spec_from_file_location` 强制加载** `report_db.py`；正常情况先走 `import`，缺函数才强制重载

### 验证（对拍 · 全部逐项相等）
| 窗口 | 计划(个/花费) | 场景(个/花费) |
|---|---|---|
| 近7天 09-09~09-15 | 7 / **189.05** | 1 / **189.05** |
| 近30天 08-17~09-15 | 8 / **388.67** | 1 / **388.67** |
| 全区间 ~09-15 | 8 / **410.98** | 1 / **410.98** |
| 自定义 09-15~09-16 | 7 / **26.88** | 1 / **26.88** |

- headless 打开 `/panel/adreport`：近7天 KPI/计划表/场景表 **均 189.05**；切自定义 09-15~09-16 → KPI **26.88**、「推广计划 自定义 09-15 ~ 09-16 合计 7 个」7 项相加 = **26.88**、场景表 **26.88** ✓
- 账户隔离回归：`account='99999999'` / `''` → 三张表全空 ✓

### 已知未改（待梓帆定）
- 「趋势解读」仍是**全区间**口径（读 `daily` 全量），切范围时文案里的「区间内 82 天」不跟随 → 要不要让它跟随范围？

### 插件
- `adreport` 0.2.7 → 0.2.8

---

## v0.8.16 · 2026-09-16（报表 KPI「今日」改读快照 · 与下方表同源）

### 修复（面板「今日」KPI 整排 0，而下方计划/场景表有数）
- **根因**：同一块「今日」用了两个数据源 —— KPI 卡读 `daily` 表里 `date=今天` 的行，下方表读 `plan_snap`/`scene_snap` 的 `tag='today'` 快照
- **为什么 daily 没有今天行**：全量采集 `_report_fetch.py` 的**日粒度趋势接口不含当天**（脚本 line 164 原注释已写明），只写 T+1 历史；`daily` 的今天行**仅由「今日采集」`_report_today.py` 写入**。今天只跑了全量（09:00 定时 + 13:29 手动 `refresh-all`）→ `daily` 无 09-16 行 → KPI 全 0
- **为什么表格有数**：全量采集**会**把今日实时数据写进 `tag='today'` 快照（`_report_fetch.py` line 248-260）→ 表格读快照有数
- **修**：`adreport/server.py` 的 `today_total` 改为**优先从 `tag='today'` 场景快照汇总**（charge/adPv/click/amt/num 求和，ctr/ecpc/cvr/roi 现算），与 `plansToday`/`scenesToday` 同源；无快照时回退 `daily` 今天行
  - 加购数 `cart`：快照表无此列 → 仍取 `daily` 今天行（今日采集会写；没跑则为 0）
- **设计取舍**：**不给 `daily` 补今天行** —— 保持 `daily` 纯 T+1 历史，符合十六段经验「历史只到昨天、走势不画今天」

### 验证
- 打 `/api/plg/adreport/report`：`today` = charge **17.10** / adPv **615** / click **35** / ctr **5.69%** / ecpc **0.489** / amt 0 / num 0
  → 与 `scenesToday`（关键词推广 17.10 / 615 / 35 / 0 / 0）**逐项相等** ✓

### 插件
- `adreport` 0.2.6 → 0.2.7

### 待办
- 加购数要有值 → 需**每天跑一次「今日采集」**(`_report_today.py`)；建议给 scheduler 加个「报表今日采集」任务（待梓帆定）

---

## v0.8.15 · 2026-09-15（流水线阶段条骨架 · 数据驱动）

### 新增（`pipeline` 插件）
- **阶段定义数据驱动**：后端新增模块级 `STAGES`（7 项 —— 查询 / 属性 / 素材 / 整理 / 人工审核 / 上品·优化 / 推广），每项含 `key / label / ready / desc`
- **`/stages`**：返回阶段定义，前端据此渲染阶段条 → **明天填血肉不用动前端**
- **`/run?stage=<key>&lhs=<料号列表>`**：统一阶段执行入口
  - 已就绪（`query` / `material` / `audit`）→ 转调原 action（`classify` / `scan_batch` / 审核提示）
  - 未就绪（`attrs` / `sort` / `pub` / `tuiguang`）→ 只回 `{ok:false, ready:false, error:"…还没接血肉｜计划调用：…"}`，**不动任何真进程**
- 前端阶段条：写死的 5 格 → 动态 7 格（`loadStages`/`renderStages`），每格可点，未接线灰显 + 悬停提示计划调用，箭头串联
- 原写死索引的调用改 key 版（`setStageKey('material'|'query'|'audit', …)`）→ 以后插入/调整阶段不会错位

### 验证
- `ast.parse` 语法通过；`/stages` 返 7 阶段；`/run?stage=pub` 只提示不动作；`/run?stage=material&lhs=…` 返回清点结果；`/run?stage=audit` 返提示
- headless 打开 `/panel/pipeline`：7 格渲染、4 格灰显、点未接线格只改文案不动作

### 插件
- `pipeline` 0.2.10 → 0.2.11

### 明天填血肉（按序）
1. **属性** → `fetch_attrs`（爬，只读不改商品，最安全）
2. **整理** → `organize`
3. **上品/优化 + 推广** → 真动作，先拿 1 品 `--no-submit` 预演
- 接线**照 `_optimize_flow.py` 拄**（它本身就是调用层范例），但**别直接调它**（一体总装会绕过人工审核闸门）

---

## v0.8.14 · 2026-09-15（报表三表对齐 + 守护进程脱离工作台）

### 修复（报表「今日」三处对不上）
- **根因 1（主因）**：`_report_today.py` 落库 `plan_snap`/`scene_snap` 时把 ts 传成 date-only `'2026-09-15'`；`report_db.get_plans/get_scenes` 取 `MAX(ts)` 是**字符串比较** → `'2026-09-15T09:00:00' > '2026-09-15'`，面板永远取 09:00 旧批（2.79/188/7），而 daily 是覆盖写已更新（22.24/868/55）
  - 修：两处 ts 实参 `today` → `None`（改用完整时间戳）
- **根因 2**：KPI 卡取字段写错 —— `ta.get("alipayInshopAmt")`，而库里字段就是 `amt`（`num`/`cart` 同理）→ 成交额/成交笔数/加购数恒为 0（实为 54 元 / 1 笔 / 6）
  - 修：改回 `amt`/`num`/`cart`
- 数据修正：把库里 tag='today' 的 date-only 脏行 ts 掰正为 `2026-09-15T16:45:56`
- **验证**：KPI 卡 = 场景合计 = 计划合计 = 22.24 / 868 / 55；成交 54 元 / 1 笔 / 加购 6

### 优化（守护进程）
- `daemon_start()` 的 Popen 补 `DETACHED_PROCESS(0x8)|CREATE_NEW_PROCESS_GROUP(0x200)`：原只加 CREATE_NO_WINDOW，daemon 实为 workbench 子进程，工作台退出/被 `taskkill /T` 会连坐
- `_daemon_alive()` 补 **pid 存活校验**（tasklist）：原来只看「3 分钟内有心跳」，刚 kill 掉 daemon 后 3 分钟内会误判为活 → `daemon start` 起不来
- 清理 3 个任务陈旧的 `last_result='running'`（tick 只认 last_run，不阻塞触发，纯显示脏）

### 插件
- `adreport` 0.2.5 → 0.2.6
- `scheduler` 0.1.2 → 0.1.3

---

## v0.8.13 · 2026-09-15（送修通道 · 真结论：seedream_5.0 = UI 的 5.0 Lite）

### 纠正 v0.8.12 的错误结论
- 上一版改“不传 model”：**实测走的是服务端默认 4.0（花积分 1/张）** ✗
- **正确**：`general_agent_settings.image_model = "seedream_5.0"` —— **= 网页版 UI 的 “Seedream 5.0 Lite”，实测不花积分** ✓
- 対时据（梓帆核对小云雀）：14:27 那次 5.0 lite 不花积分 = 本侧 14:26 跑的 `seedream_5.0`；14:32 “不传 model” 那次 = 4.0

### 最终设置
```python
def submit(message, asset_ids, model="seedream_5.0"):
    body = {"message": ..., "asset_ids": ...,
            "general_agent_settings": {"image_model": model}}
```

### 教训
- **报时间先对表**（本次因未对表，把 14:26 误以为 17:5x，推出错结论）——梓帆定过的规矩，又忘
- 对比实验要固定变量：一会儿改位置、一会儿改不传，变量太多无法归因

### 插件
- `pipeline` 0.2.9 → 0.2.10

---

## v0.8.12 · 2026-09-15（送修通道 · 最终结论：不传 model）

### 结论（绕了四轮）
1. 原代码传**顶层** `"model"` → **被服务端静默忽略** → 实际用服务端默认 = **5.0 lite（不花积分）**
2. 改成“正确位置” `general_agent_settings.image_model` 后，服务端才真读：`seedream_5.0_lite` / `doubao-seedream-5-0-lite-260128` **都报“不支持”**
3. 服务端白名单（skill API）：`seedream_5.0 / seedream_5.0_pro / seedream_4.3 / nova2 / nova_pro / seedream_4.5 / seedream_4.1 / seedream_4`
4. 网页版的 `doubao-seedream-5-0-lite-260128` 是**另一条通道**，skill API 不收

### 最终改法
- `ai_schedule.submit(model="")` —— **默认不传模型**（=服务端默认 = 5.0 lite，不花积分）；传时仍必须放 `general_agent_settings.image_model`
- 实测：不传 model 送修 → 45s 出图 ✓
- 两份 `ai_schedule.py` 同步

### 校验
- 服务端报错信息自带白名单，是确认支持模型最快的办法（不用猜）

### 插件
- `pipeline` 0.2.8 → 0.2.9

---

## v0.8.11 · 2026-09-15（送修通道换 seedream_5.0_lite）

### 优化
- **送修（clean）通道** 从 `seedream_4.5` 换成 **`seedream_5.0_lite`**（4.5 会花积分；5.0 lite 不花）
  - `ai_schedule.submit(model=...)` 默认值改；**两份 ai_schedule.py 同步改**（推广一键跑 + 插件 scripts）
  - **实测跑通**：1379029 clean → 2 张（130s，纯白单主体验证通过）
  - 注：**CLI 文档里没有 `lite`**（只有 4/4.1/4.3/4.5/5.0/5.0_pro）——UI 显示 “Seedream 5.0 Lite” 对应 API 名 `seedream_5.0_lite`，服务端已实收

### 插件
- `pipeline` 0.2.7 → 0.2.8

---

## v0.8.10 · 2026-09-15（产物命名冲突：换源 vs 送修）

### 修复（设计问题，非 bug）
- **换源产物不该叫“白底”**：`switch_src.py` 立创分支产物名与 `repair_batch.py`（送修）**完全同名** `_白底_待复检`，两个工序产物混在一个筐里，互相冲突
  - 修：换源产物 → `{lh}_换源_待复检{ts}.png`；送修保持 `_白底_待复检`（图库分支原本就叫 `_库源N_`，只有立创分支漏了）
  - 存量改名：1379029 的 5 张 `_白底_待复检` → `_换源_待复检`
  - 实测下游兼容：`_ref_for(送修)`、`ref_img` 都能匹配新名（都按 `*_待复检*` 找）

### 规范（从此遵守）
- **产物命名 = 工序名 + _待复检 + 时间戳**：`_库源N_`（图库）/`_换源_`（立创）/`_白底_`（AI 送修）——不同工序绝不共用名字

### 插件
- `pipeline` 0.2.6 → 0.2.7

---

## v0.8.9 · 2026-09-15（待复检/送修看不到参考图 · 修）

### 修复
- **标了“送修/换源”的品，待复检点进去看不到图**：`server._ref_for()` 只在 `audit == "待复检"` 时看产物；而换源/送修后 state 里的 audit 是“送修”/“换源” → 掉到“找原图”分支 → 没原图就空
  - 修：`if audit and audit != "放行"` —— **在途品（待复检/送修/换源）都先看产物**
  - 实测：1379029 在 送修/待复检/换源 三种状态下都能拿到 `白底_待复检` 图

### 经验
- **同一个概念在代码里有多个“名字”时，状态判定极易漏**（“在途品”写成了 `== "待复检"`，忘了还有“送修/换源”）——能用“非放行”这种**否定式判定**就不要枚举正向值

### 插件
- `pipeline` 0.2.5 → 0.2.6

---

## v0.8.8 · 2026-09-15（审核类型丢失/计数/送修参考图 · 浏览器锁补全）

### 修复
- **审核后类型被抹成“查询异常”**：`classify` 结果**不落 state** → 前端每次审核都重查；且“查询异常”会被写进 state 永久覆盖
  - 修：`classify` 落 state（type/id/title）；**“查询异常”不覆盖已有 type**
- **换源计数 2/4（应为 1/3）**：state 里混着**测试料号 `XX99-NOT-EXIST`**（它 audit=换源）→ 分子分母各 +1；已清
- **送修秒退回**：不是通道堵——`repair_batch.ref_img()` 把“白底/待复检/待复核”全排除，而 1379029 只有换源产物 → “无参考图—跳过”
  - 修：`ref_img` 兜底——无原图时拿最新“待复检产物”当参考（与审核预览口径一致；clean 模式不重绘，安全）
- **插件侧（分流/送修/换源）从来没浏览器锁** → 与推广/采集抢 profile 必撞
  - 修：`browser_lock` 锁路径**固定到「推广一键跑\runtime」**（原按脚本目录→插件副本会锁到另一个文件）；`_classify_one.py` 接入 `guard("流水线分流")`

### 经验
- **try/except + 默认值是隐形杀手**（今日本次第三次实锤）：profile 回退 `.profile`、BOM 致插件消失、查询异常覆盖 type——都是“错了但不说”
- **同一平台两条路行为不同**：先直接打印两边实际用到的路径/模块来源（`__file__`/`PROFILE_DIR`）对比，比读代码快

### 插件
- `pipeline` 0.2.3 → 0.2.5

---

## v0.8.7 · 2026-09-15（流水线登录态真正修好 · 根因）

### 修复
- **流水线「查询分流」被卡手机验证（真根因）**：`_mtop_api._profile_dir()` 里的 `import acct_profile` —— `acct_profile.py` 在「推广一键跑」，而插件那份的 `BASE` 指向插件目录 → **import 失败 → 静默回退 `.profile`（无登录态）**
  - 修：把「推广一键跑」也加进 `sys.path`；“回退 .profile”从默认行为变成“真的拿不到才用”
  - 实测：插件 `PROFILE_DIR = ...chrome_profile`，分流 `ok=True 8s`（老品-双百跳过）
- 分流改**子进程**跑（`scripts/_classify_one.py`）：playwright 在内核请求线程里不稳（拿不到 `_m_h5_tk`）

### 经验
- **“静默回退”是隐形杀手**：`try/except` + 默认值（.profile）会把“配置没读到”变成“默默用错的”（与 BOM、落库同一个病根）
- 插件自包含脚本的 `BASE` 与主脚本不同——依赖“其他目录里的模块”（acct_profile/conn_store）时必须显式加入 `sys.path`

### 插件
- `pipeline` 0.2.2 → 0.2.3

---

## v0.8.6 · 2026-09-15（流水线登录态修正 · 副本同步）

### 修复
- **流水线「查询分流」要验证码**：`pipeline/scripts/_mtop_api.py` 是从主脚本复制的**旧副本**，仍用 `BASE/.profile`（无登录态）——而主脚本那份已改成“跟激活连接”
  - 同步副本 → 两边一致；实测 `PROFILE_DIR = ...chrome_profile`（有登录态）
  - 同因：`plugins/pipeline/scripts/.profile` 就是这份旧代码生成的（前晚误入库的 271 文件）
- `tuiguang_auto.py`：`wxt_login_if_needed` 里“已在推广页”却 `return False`（语义反了，原版就错，被早停引爆）→ 已修为 True；登录闸门已回滚

### 经验
- **脚本有副本时，改主脚本必须同步副本**（流水线插件自包含 `scripts/` 目录）——否则同一个平台两条路会用不同登录态

### 插件
- `pipeline` 0.2.1 → 0.2.2

---

## v0.8.5 · 2026-09-14（scheduler 可接替 Hana 定时任务）

### 新增
- `scheduler` 的 `promo_batch` 动作：**计划组留空 → 自动取该账号「未满（count<500）」的计划**（读 `plan_state_<member>.json`）；取不到则拒绝执行并提示先刷新容量

### 说明
- 工作台任务不经过助手（**不烧 token**），可接替 Hana 宿主层的定时任务
- 已建三个推广任务（默认停用，验证后再开）：晚间批 `0 19-23 * * 1-6` / 周日全天 `0 0-23 * * 0` / 凌晨批 `0 0-4 * * 1-6`

### 插件
- `scheduler` 0.1.1 → 0.1.2

---

## v0.8.4 · 2026-09-14（双百 / 流水线 按账号）

### 新增
- `dual-check` / `pipeline` 面板顶部显示**当前账号**（连接名 · member_id）；两者新增 `acct` 接口

### 修复
- `pipeline` 推广记录：
  - 路径指向**错误文件**（`推广一键跑\推广记录.csv` → 实际在 `办公室工作\数据\推广记录.csv`）
  - 用 `DictReader` 读**无表头** CSV → 首行被当表头 → **恒返回空** → 改 `csv.reader` + 按账号过滤

### 插件
- `dual-check` 0.2.0 → 0.2.1
- `pipeline` 0.2.0 → 0.2.1

---

## v0.8.3 · 2026-09-14（修 manifest BOM · 插件消失）

### 修复
- **`tg-monitor` 插件“消失”**：改 manifest 时用了 `utf-8-sig` 写出，文件带了 BOM；内核 `scan_plugins` 用 `json.load(open(mf, encoding="utf-8"))` 读 → **BOM 导致 JSON 解析失败 → 静默跳过** → 插件列表里没了（目录完好、内容正常）
  - 修为 utf-8 无 BOM；已逐个核查全部 6 个插件 manifest 无 BOM

### 经验
- **改 manifest 必须 utf-8 无 BOM**
- **`except: pass` 的静默失败要留痕**（今天第二次栽在同一件事：落库的 try 吞异常、扫描器的 except 吞解析错误）

---

## v0.8.2 · 2026-09-14（推广数据全按账号）

### 优化
- **刷新容量重写**（`refresh_caps.py`）：先拉**计划列表**（`campaign/findList.json`）→ 再逐个查单元数；profile 跟激活连接；写 `plan_state_<member>.json` / `plan_names_<member>.json`
  - 原版写死 3 处（默认 profile / 全局文件名 / **店铺1 的 11 个计划 ID**）——“有推广但不是我们推的”账号拉不到
  - csrf 抓法**照抄** `_report_fetch.py`（先过卖家中心 + `login_qianniu`）+ 补 `bizCode=onebpSearch`
- **推广记录加 account 列**（`料号,状态,时间,账号`）：`tuiguang_auto` 写入带账号，去重按 (料号,账号)；存量 2589 行已补列
- `tg-monitor`：**已推记录 / 今日 +N / 计划列表 / 容量** 均按当前账号；面板顶部显示账号

### 插件
- `tg-monitor` 0.2.0 → 0.2.1

---

## v0.8.1 · 2026-09-14（任务计划打磨 · 文件拖拽上传）

### 新增
- 内核插件 **POST 通道**：`POST /api/plg/<插件>/<动作>` —— `application/json` 当参数，其它当原始字节（插件可传文件）
- `scheduler` 文件参数支持**拖拽**（任意位置的文件都行，上传到 `data/uploads/`）+ 已存文件下拉选择

### 修复
- `scheduler` 时间判定：**输入标准化**（全角冒号/逗号/空格）——`16：04` 也能触发；判定端同样容错老数据
- `scheduler` 守护单例：改 **文件锁**（`msvcrt`，进程退出自动释放）——修多实例并存
- `scheduler` 面板：新建/取消拆成两个函数（原来共用导致点“新建”反把表单藏起来）
- `scheduler` 文件拖拽：原“按文件名在固定目录找”改为**真上传**——不再圈目录

### 插件
- `scheduler` 0.1.0 → 0.1.1

---

## v0.8.0 · 2026-09-14（任务计划插件 · 报表去 JSON · T+1）

### 新增
- **新插件 `scheduler`（任务计划 🕒）**：定时驱动工作台功能，到点自动执行、不经过助手
  - **六种触发**：间隔 / 一次性 / 每天（可多时间点）/ 每周（选星期）/ 每月（选号或月末）/ Cron
  - **动作注册表**：报表·全量采集 / 报表·补今日 / 流水线·推广一批 / 查双百·批量
  - **批处理可传参**：数据文件 / 每批数量 / 关联账号——多店铺多门类各建各的
  - **断点续跑**：跑完写进度，下次自动接上
  - **独立调度守护** `daemon.py`（零依赖）：不依赖工作台常驻；单例；同分钟不重触发；一次性跑完自动停用
- 内核新增 `/api/account-token`（账号指纹）——供插件前端轮询、切号自动跟随

### 优化
- **趋势窗改 T+1**：历史截至**昨天**，今天的数据未定稿、不进走势
- 采集：今日场景/计划也落库（`tag='today'`）
- 登录凭据统一走 `acct_profile.cred()`（连接库优先）；`_mtop_api` 的 profile 跟随激活连接
- 内核 `browser-login` / `browser-clear` 改为跟随激活连接（原来写死默认 profile）

### 修复
- **报表切号串台**：场景/计划、总计、今日计划/场景原本从 JSON 读（单份文件、跨账号覆盖）→ **全部改走库**（带 `account` 列）
- **口径不一致**（总计含今日、场景不含）：场景/计划区间 = 库快照 + `today` 快照合并
- **日期窗口按条数取**：`slice(-7)` 取“最后7条”而非“近7天”，缺日就错 → 改按日期窗口 + 缺日补 0
- **刷新分流只看最后一天**：中间断档看不见（死机漏采永不补）→ 加**缺口检测**（近30天）；再加**新鲜度检测**（过去某天“当天就采”的记录=未定稿→重采；**今天除外**）
- **落库静默失败**：`daily` 表老定义 `date PRIMARY KEY` → 多账号日期撞车、且 `raw` 变量未定义被 try 吞 → 重建表为 `UNIQUE(account,date)` + 失败写日志
- 采集落库连累回填：member_id 回填改在落库之外（落库失败也能绑上）
- scheduler 面板：新建/取消拆成两个函数（原来共用 `editTask(null)`，点“新建”反把表单藏起来）

### 插件
- 新增 `scheduler` 0.1.0
- `adreport` 0.2.4 → 0.2.5

---

## v0.7.5 · 2026-09-10（采集认账号 · 今日落库 · 智能刷新）

### 修复
- **采集不认激活连接**（切号后仍采老账号的数据）：采集脚本的浏览器 profile 由写死改为**跟随激活连接**
  - 新增 `acct_profile.py`：连接配了 `profile` 用它；否则推导 `chrome_profile_{连接名}`；**老号（已绑 member_id）沿用现有 `chrome_profile`**——不折腾已登录的号
  - `_report_fetch.py` / `_report_today.py` 支持 `--profile` 覆盖，并把用到的 profile 回填到连接（看得见、可手改）
- **走势停在昨天**：`_report_today.py` 采完今日**同步落库**（当日反复覆盖、次日定稿）——走势连续到最新一天
- **刷新只管今日、新号不采历史**：`refresh` 改**智能分流**——库缺该账户存档/断档 → 全量采集；存档已齐 → 只补今日（省时间）
- 接口 `report` 新增 `stale` 字段；前端据此提示「已切到该账户的本地存档」/「该账户还没有数据」

### 插件
- `adreport` 0.2.4 → 0.2.5

---

## v0.7.4 · 2026-09-10（切号即隔离 · 按激活连接取数）

### 优化
- **面板改按「激活连接」取数**（不再按“上次采集的账户”）——切换账号后**一律不显示别的账户数据**：
  - 激活连接有本地存档 → **秒刷新**（日走势/计划/场景/总计全部从库读该账户）
  - 激活连接没存档（新号未采集）→ **面板清空** + 提示「点刷新采集」
- `report_db` 新增 `get_plans()` / `get_scenes()`（按账户取最新快照）
- 接口新增 `stale` 字段；前端据此提示「已切到该账户的本地存档」/「该账户还没有数据」
- 实测：正常态 `stale=False`；切到未采集连接 `stale=True, days=0`；恢复后自动回 `days=89`

### 插件
- `adreport` 0.2.3 → 0.2.4

---

## v0.7.3 · 2026-09-10（账户标识 · 动态绑定 · 一致性自查）

### 新增
- adreport 面板顶部显示**账户标识**：`连接名 · 账号 memberID　|　数据天数　采集时间`
  - 连接名取**连接导航当前激活的店铺连接**（`conn_store.active_of("taobao")`）——**动态**，切号即变，不写死
- **账户一致性自查（即时感知）**：连接表新增 `member_id` 列（采集后自动回填绑定）——面板对比「激活连接绑定的账号」与「当前数据的账号」，**不一致立即红字告警**，不再出现“显示新名、数据旧号”
- **内核版本自动跟随**：`/api/plugins` 新增 `core`（从 `CHANGELOG.md` 首条动态读）——About 面板不再写死，**永不滞后**

### 插件
- `adreport` 0.2.2 → 0.2.3

---

## v0.7.2 · 2026-09-10（报表账号隔离加固）

### 修复
- adreport 读取侧账号兜底漏洞：`get_daily(account=_acct or None)` 在取不到 memberId 时**放开全库**——多账号上线后 A 号面板会看到 B 号历史（串台）
  - 新增 `_resolve_account(raw)`：trend → account 两级回退，与采集侧 `_report_fetch.py` 对齐
  - 取不到账号 → **不查库**（返回空，回退 JSON），绝不 `account=None`
  - 实测：空账号查库 0 行；按不存在账号查 0 行；正常路径 days=89 不变

### 插件
- `adreport` 0.2.1 → 0.2.2

---

## v0.7.1 · 2026-09-10（报表数据本地库）

### 新增
- adreport 数据**入库累积**（`推广一键跑/report_db.py`，sqlite3 标准库 → `runtime/report.db`）
  - `daily` 账户按日（upsert，历史越攒越长）· `plan_snap` / `scene_snap` 区间快照
  - 防平台数据过期/被删；**读取优先走库**（秒回），缺数据才采集
  - 库文件在 `runtime/`（gitignore 保护，业务数据不入仓）

---

## v0.7.0 · 2026-09-10（两款主题 + 万相台报表插件）

### 新增
- **新主题 `苍银`（夜用）**：夜蓝底 × 金印，深色高对比——适合夜间/暗环境；金只用于强调与激活（#C9A227），圆角 4px 方正
- **新主题 `晓蓝`（日用）**：素雅浅蓝 + 磨砂玻璃（`backdrop-filter`）——晨光渐变底 + 半透面板，适合办公室明亮环境；强调蓝调浅（#5E8AB4）
- **主题区改版**：3 列卡片 + 每套一句介绍（照 Hana 风格），布局不再拥挤；主题变量经 `wb-theme` 广播，插件 iframe 自动跟随
- **新插件 `adreport`（报表 📊）**：万相台报表台，把要翻很深才看得到的数据拉平到一处
  - 账户概览 10 指标（花费/成交额/投产比/展现/点击/CTR/平均点击花费/转化率/成交笔数/加购）
  - **指标可点切换走势**（点哪张卡，折线图换主角）；**本期 vs 上期**对比（近7天/近30天/自定义≤30天）
  - **推广计划表（11 个计划）** + **营销场景表**，**均跟随时间范围**切换
  - **今日**范围（走实时接口 `fromRealTime`，日粒度不含当天）
  - 趋势解读（高点 / 挂零日 / 投产比 / 场景集中度）
  - **采集方式**：面板「刷新」= 后台**静默采集**（headless，不弹浏览器）；**惰性定时**（每天 9:00/21:00 后首次打开自动采）
- 内核 API：插件 server `report` / `refresh` 两个 action

### 修复
- 报表插件：表头右对齐、数据左对齐导致的**列错位**（`td` 补 `text-align:right`）
- 报表插件：今日各计划误用账户总计（漏 `strategyCampaignIdIn`）；今日场景误用全区间——已修，并加采集自检（各计划数值全同则告警）

---

## v0.6.0 · 2026-09-10（连接导航 · 凭据管理）

### 新增
- **新插件 `odoo-erp`（ERP 数据）**：Odoo **只读**查询——料号 → 售价/成本/库存/品牌/描述/分类（仅 authenticate/search_read/search_count，绝不写生产库）
- **连接导航**（设置 → 连接）：多连接管理（淘宝店铺 / Odoo ERP），可新增/编辑/激活/测试/删除
- **凭据加密**：密码走 **Windows DPAPI**（ctypes CryptProtectData——可逆、绑当前用户、零依赖）——**绝不明文/不用 MD5**（MD5 不可逆，登录需明文，不可用于此）
- **存储**：`data/conn.db`（sqlite3 标准库，零下载）
- **Odoo ERP 测试连通**：`xmlrpc.client` 标准库直连 `/xmlrpc/2/common` 认证——为接入公司 ERP 产品数据铺路（替代爬取）
- 内核 API：`/api/conn/list|save|del|activate|reveal|test`

---

## v0.5.0 · 2026-09-09~10（送修/换源闭环 + 图库接入）

### 新增
- **素材审核（预览 Tab）复审模式**：待复检品自动显示送修/换源产物 + **⇄ 对比原图**按钮（ref=产物 vs orig=原图）；键盘 ←/A/→/D 切品、**空格**=对比、**F**=force、1/2/3=放行/送修/换源、Enter=确认；审核激活自动拿焦点（不用先点预览）
- **送修闭环**：审核标送修 → 🔁 送修处理（repair_batch v2 **并行**并发 3）→ AI clean 模式（去标注保留原样）→ `_白底_待复检` 落素材 → 标待复检 → 复检。workbench 固定进度条 5s 轮询（送修中 N/M + 当前品 + 失败数）
- **换源闭环**（switch_src）：四源递进 **① 本地图库 `F:\连接图图库` → ② TE 官网 → ③ RenhotecRF → ④ 立创**；命中落 `_库源N_待复检`/`_待复检`；Renhotec 顺手抓 Drawing PDF 当规格书
- **本地图库索引**（lib_images，13.2 万图 / 4.4 万料号，0.9s 建）：料号归一化匹配（连字符无关），查询/审核/换源三处接入
- **单品查询走分流**：单料号回车 = 搜商品 + 查双百 + 素材 + type/audit（不再是纯本地查询）
- **批锁**：送修/换源互斥——一个批在跑时再启动会被拒（防进度文件互覆盖）

### 修复
- **审核 0 素材 bug**：素材目录无图时 `_ref_for` 加图库兜底——审核仍能看到产品图（不再空白）
- **审核 ref 过期快照**：审核启动时对在途品（待复检/换源）现场重查 `audit-ref`——送修完成后不再显示旧图
- **skip 品复检被挡**：双百跳过但有在途标记（送修/待复检/换源）的品不再自动跳过
- **classify 缺 audit**：分流结果带出 audit/ref/orig——送修/换源按钮正确联动
- **lcsc.py 域名拼错**：`item.slcsc.com` → `item.szlcsc.com`（立创通道长期失效真凶）

### 修复
- **tg-monitor 登录态持久化**：内核 call_plugin 每次请求重载 server.py → 模块级缓存丢失（点了检测仍显示"未检测"）→ 登录态落文件 `login_state.json`
- **odoo-erp 精确匹配开关常驻**：原来随结果数消失（结果变 1 条就回不去）→ 钉在查询栏

### 优化
- 素材列显示：只缺图且有库源 → 标 `缺:主图[库N→可补]`（不算硬缺）
- **架构：脚本入插件**（`plugins/pipeline/scripts/`）——repair/switch/lib_images/renhotec/ai_schedule/lcsc/_grab_te_img/_mtop_api 自包含（TG 硬编码清除）；工作台不再依赖外部仓
- 版本号跟随：内核 v0.5.0 / 插件 pipeline·dual-check·tg-monitor 0.2.0
- runtime 脚本入库（gitignore 白名单 `!runtime/*.py`）

---

## v0.4.0 · 2026-09-08（主题全局化 + 推广监控工程化）

### 新增
- **主题全局跟随机制**：宿主切主题 `postMessage` 广播 CSS 变量 → 插件 iframe 接收应用——三插件全部跟着主题走（纸色 fallback 保底）
- 新主题：**深邃蓝**（delve 冷白+纯黑高对比致敬）、**莫兰迪**（雾蓝灰低饱和）——含组件级形态覆盖（圆角/阴影/层次）
- **推广监控台插件**（tg-monitor）：
  - 11 计划组真名卡片 + 容量实时（probe 拉登录态连带刷新 + 「刷新容量」按钮）
  - **推广工程区**（与监控区隔离）：单品精准推广（料号/ID）/ 批量（拖 csv 或默认池）/ 数量可调 / 停止
  - 默认池进度（共 4284 / 已跑 / 剩）+ 本次批料号清单 + 批结束自动提示新增数
  - 起停批 PID 方案（tasklist 检测，跑批状态可靠）
- 查双百单查重排（卡片式上下布局 + 整宽查按钮）

### 修复
- 推广假成功三连根因（脚本侧，详见 taobao 仓）——本面板口径已按权威记录显示
- Chrome autofill 灰蓝底盖输入框 → `-webkit-autofill` 强制跟随主题底色
- 插件反复 patch 遗留的重复函数/写死色 → 变量化清理
- 登录态探测卡死进程残留导致连环失败（profile 锁）——进程清理 + 90s 缓存
- col-desk / 面板区域特殊白 → 统一 `var(--card)`（区域不再掉队）

### 优化
- 插件 iframe 缓存并存（切换不销毁——各插件独立存活不重载）
- 插件 icon 生效（manifest `icon` emoji）
- 拖放区加大（130-140px）三插件统一
- 流水线阶段灯居中布局

---

## v0.3.0 · 2026-09-07（工作台成型 + 独立仓库）

### 新增
- 四区布局工作台（书桌/工作区/运行日志/预览）+ 设置体系（主题/字体/字号/编辑器 md 参数/浏览器登录态/通用自启/插件开关）
- 插件体系：zip 拖入即装（manifest+panel+server+README）、热插拔、server 动态加载
- 查双百 v2：单查（料号/ID）+ 批量拖表（csv/xlsx 自动识别料号列 → 后端单连接逐行查 → 进度 + 总结）
- 预览全家桶：图片缩放拖动 / PDF fitz 转图 / CSV 表格 / MD 渲染
- 编辑器 Markdown 参数：字体/字号/宽度/H1-3/行高/边距/恢复默认
- 独立仓库 `taobao-workbench`（公开）+ 本地 git

### 修复
- 宽度按钮"点不动"（重复 edSeg 定义覆盖 → onclick 拼错）
- manifest `panel` 字段笔误（`entry` → 点开 404）
- 误删 edSet/edNum/EDKEY 补回、重复声明清理

---

## v0.2.0 · 2026-09-06（内核对齐 + 插件雏形）

- 内核搜索/文件 API / 书桌素材联动（料号 → 显示素材）
- 查双百插件雏形（单查）
- 设置-浏览器：登录态检测/引导登录/清除 Cookie

---

## v0.1.0 · 2026-09-05（内核起步）

- workbench.py 标准库内核（8900 端口）+ index.html 初版
- 书桌文件浏览/预览基础
